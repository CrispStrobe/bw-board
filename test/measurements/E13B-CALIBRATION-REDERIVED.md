# E1.3b: the LED calibration, re-derived against a real oracle

**Measured 2026-09-12.** bw-board `lane/e13-junction-routing-measured`, ngspice-42,
`.options TEMP=27 TNOM=27`.

## Why this was re-done

Every LED accuracy claim in this repo was scored against `test/golden/oracles.json`.
That file is generated by `compute_oracles.py`, its LED entries carry an `"rd": 10`
field, and its expected current for `led_blue_470` is exactly
`(5.0 - 3.2)/(470 + 10 + 25)`. **It is the piecewise model written down.** Its header
note "computed independently from bw-board solver" is true of the *solver* and false
of the *model*, so the only LED oracle in the tree could never disagree with the
piecewise path however wrong that path was.

`test/golden/ngspice_oracles.json` has carried three real LED measurements
(`led_red_220`, `led_red_1000`, `led_blue_470`) the whole time. **No test consumed
them.** An unread oracle is worse than an absent one, because its presence is cited
as coverage.

## Three findings, in dependency order

### 1. The solver was never the problem

With `is`/`n`/`rs` injected so both engines hold the **identical** device:

| case | ours | ngspice | error |
|---|---|---|---|
| led_red_220 | 13.29667 mA | 13.29170 mA | 0.04 % |
| led_red_1000 | 3.09511 mA | 3.09405 mA | 0.03 % |
| led_blue_470 | 4.06541 mA | 4.06184 mA | 0.09 % |

The residual is not a fit quality. It is `VT_25C = 0.02585` — which is really
**26.83 °C**, not 25 °C — against ngspice's 300.15 K value 0.0258649, 0.058 % apart.

### 2. `rs` and `rd` are one quantity, and they disagreed

`rs` defaulted to **2**; the piecewise path uses `rd = 10` (`board.js` `LED_RD`, plus
seven `const rd = 10` sites in `mna.js`). So `JUNCTION_ROUTING` was not switching
models of one device — **it was switching devices.**

The 2 was not measured against a part. It minimised error against `oracles.json`,
i.e. it was fitted to the model being corrected.

**Do not re-tune `rs` against this corpus.** A sweep elects whatever `RS` the
reference devices were built with:

| reference built with | rs=2 | rs=5 | rs=10 | rs=25 | rs=40 | elected |
|---|---|---|---|---|---|---|
| RS=5 | −0.59 % | **+0.04 %** | +1.05 % | +3.84 % | +6.31 % | 5 |
| RS=10 | −1.75 % | −1.07 % | **+0.04 %** | +3.08 % | +5.78 % | 10 |
| RS=25 | −6.08 % | −5.22 % | −3.82 % | **+0.04 %** | +3.45 % | 25 |
| RS=40 | −11.68 % | −10.62 % | −8.90 % | −4.17 % | **+0.04 %** | 40 |

A perfect diagonal, because `shockleyParams` **algebraically reconstructs** the
device when `rs == RS`: `vJrated = vf − 0.020·rs = Vj(20 mA)`, so
`is = 0.020/(exp(Vj/nVt) − 1) = IS`. Not a good fit — the same device.

`rs = JUNCTION_RD` is therefore justified by the **identity with `rd`**, not by any
sweep. Held by `test/junction-rs-equals-rd.test.mjs`.

### 3. `vf` means two different things

At the rated bias (5 V, R=150, vf=2.0 — by definition the 20 mA point):

    piecewise   18.7500 mA        exponential   20.0000 mA

The piecewise path treats `vf` as the **knee** and adds `rd·I` on top, so its total
drop at 20 mA is `vf + 0.2 V`. The exponential path calibrates so the **total drop**
is `vf` at 20 mA — the datasheet convention, `Vf @ If = 20 mA`.

Against the ngspice references:

| case | truth | piecewise now | piecewise, knee = `vf − 0.020·rd` |
|---|---|---|---|
| red R=220 | 13.2917 mA | −7.13 % | −0.58 % |
| red R=1000 | 3.0941 mA | −9.14 % | −2.74 % |
| blue R=470 | 4.0618 mA | −14.39 % | −4.14 % |

**Most of what E1.3b credited to the exponential is the convention, not the knee.**

## The re-derived expectations

ngspice truth for a datasheet `vf = 2.0` LED (`IS=3.1657e-19 N=1.8 RS=10`):

| failing test | truth | current expectation | error | `rs=2` | `rs=JUNCTION_RD` |
|---|---|---|---|---|---|
| Pico 3V3 → 100 Ω | 13.7602 mA | 11.7960 mA | **−14.3 %** | −3.4 % | **+0.1 %** |
| od LOW, 1 kΩ + 25 Ω pad | 1.5629 mA | 1.2560 mA | **−19.6 %** | −8.9 % | **+0.1 %** |
| od HIGH + 10 kΩ pull-up | 0.1567 mA | 0.1181 mA | **−24.6 %** | −8.9 % | **+0.1 %** |

Corrected values, should the fleet adopt the datasheet convention:

    Pico brightness      0.5898        -> 0.6885
    od LOW  current      1.256038 mA   -> 1.5639 mA   (brightness 0.06280 -> 0.07820)
    od HIGH current      118.0745 uA   -> 156.69 uA   (brightness 0.005904 -> 0.007835)

The blink-bench and Thévenin cases derive from the same on-state current.

**The +0.1 % column is partly the identity of §2** — the reference device is
reconstructed. The independent evidence is §1 (solver vs ngspice on the corpus's own
devices) and the `LED_RD` identity. Do not quote it as a fit.

## The bulk resistance is per kind, and that is what makes the conversion work

**The conversion subtracts `iRated × rd`, so sharing one `rd` across kinds
subtracts the wrong voltage.** `rd = 10` is an LED's bulk resistance. Our own
reference silicon part — `D1N4148 D(IS=2.52e-9 RS=0.568 N=1.752)` in
`test/golden/run_ngspice_diode.py` — has 0.568 Ω, so its true bulk drop at 20 mA
is 11 mV, not 200 mV.

Measured on a bare diode, 5 V through 1 kΩ, against ngspice's **0.6532 V**:

| | anode | error |
|---|---|---|
| `vf` treated as the knee, `rd = 10` (before any change) | 0.7426 V | +13.68 % |
| converted, `rd = 10` | 0.5446 V | **−16.63 %** |
| converted, `rd = SILICON_RD = 0.568` | 0.6793 V | **+4.00 %** |

**How to reproduce these four numbers, because three of them cannot be recovered
from this description alone.** A reader who re-derived the table from the prose
got rows 1 and 2 exactly — they need only the device and the piecewise formula —
and missed rows 3 and 4, reconstructing 0.5063 and 0.6517 by guessing `vf = 0.7,
n = 1.0`. The guess is reasonable and wrong, because rows 3 and 4 are **solver
readings, not hand formulas**, and depend on which junction path the part routes
to. A measurement nobody can re-run is a claim, so:

- **Row 1 (truth)** — ngspice, `.options TEMP=27 TNOM=27`:

      V1 1 0 5
      R1 1 2 1000
      D1 2 0 D1N4148
      .model D1N4148 D(IS=2.52e-9 RS=0.568 N=1.752)

  Read `v(2)`. Note the deck's first line is the TITLE; a deck starting with
  `V1 …` silently drops the source and every node sits at 0 V.

- **Rows 2, 3 and 4** — `BoardImpl`, reading `nodeVoltages.get('n_a')` after
  `advanceTo(1_000_000n)`, on the netlist in
  `test/gallery-kind-models.test.mjs` "a bare diode is silicon (0.7 V)":
  vcc 5 V → 1 kΩ → `{kind: 'diode', params: {}}` → gnd. Defaults decide
  everything else: `vf` from `kind === 'diode' ? 0.7 : 2.0`, `rd` from
  `junctionRd(part)`, and the model from `junctionModelOf`.

  Row 2 is that reading before this lane, row 3 with `kneeFromVf` applied at the
  shared `rd = 10`, row 4 with `junctionRd` returning `SILICON_RD`.

Shas: rows 2–4 on `lane/e13-junction-routing-measured`; row 4 is the current
behaviour at that lane's head.

The middle row is the point: applied with the LED's `rd`, the vf correction made
silicon **worse than doing nothing**. The per-kind split is not tidiness — it is
the difference between a correction and a regression, and the shared `rd` was the
reason the correction looked like one.

**How it was caught, because the method matters more than the number.** The
suite's bare-diode expectation moved, and the easy reading was "another piecewise
number that shifts with the convention, re-derive it". Driving the real device
through ngspice instead showed the shift went the wrong way and was larger than
the error it replaced. *A hand-computed expectation moving is not evidence the new
value is better. Only the oracle is.*

Consequence elsewhere: the flyback clamp is now harder (−1.190 V where the
threshold was calibrated to the old `rd = 10` clamp near −1.7 V). A real 1N4148
clamping 100 mA sits near −0.85 V, so the new value is closer to the device and
that threshold is another number calibrated to the old bulk resistance.

## What is NOT done here

- **The knee correction across all readers.** A census (session lego-38) found **11
  of 12** live `vf` readers use the knee meaning and 1 the datasheet meaning, while
  **3,545 explicit corpus values are datasheet-shaped** (1,766 × `vf: 2`, 90 × `0.7`;
  nobody wrote `0.6` for a 1N4148). Correcting the knee moves every corpus part
  *toward* the meaning its value was written in — and bw-circuit-ui's SPICE exporter
  and importer **already implement the datasheet convention**, with a header telling
  users it "matches the engine" while the engine ran the knee. So the correction
  removes a shipped split rather than creating one. It must move readers 3, 4/5,
  6/7/8, 9, 10, 11 **together** or `nodeVoltage` and `branchCurrent` disagree.
- **The optocoupler** (`analog-ics.js:214`) hard-codes `rd = 50`, so its knee gap is
  **1.0 V**, five times every other part's. It cannot ride along with a blanket change.
- **Silicon `rs`.** Shared with `kind === 'diode'`; our own reference is
  `D1N4148 D(IS=2.52e-9 RS=0.568 N=1.752)`, so 10 is 17× high (~94 mV at 10 mA). The
  piecewise path has the identical error, so the two paths still agree — splitting only
  `rs` would trade a shared visible error for a silent disagreement. Fix `rd` and `rs`
  together, and re-derive the diode corpus.
- **`mna-diode.test.js:33`** uses `(5 - 2)/(220 + 10)` as the oracle for the solver
  test — the piecewise formula one layer down, with a 1 mA tolerance a knee correction
  would land just inside.

## Oracle integrity

ngspice **silently clamps diode `IS` at 1e-28**. `led_blue_470` asked for 1e-30, and
its recorded `v_anode = 3.090935` reproduces bit-identically at 1e-28, 1e-30 and
1e-35 — the deck and the number had stopped being about the same diode. At n=1.8 no
LED above **~2.86 V** can be oracled here at all; real 3.5 V blue/white parts must be
left out rather than faked. Fixed on `lane/ngspice-is-clamp`: the generator fails
closed, every reading now records its netlist, and regeneration is byte-identical.
